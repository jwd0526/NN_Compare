# Configuration for temporal benchmark experiments
# EPOCHS: To change the number of epochs, modify the epoch field in the hyperparameters section (around line 38).

# System settings
pytorch_seed: 42
cudnn_deterministic: true

# Experiment settings
experiment:
  name: "synthetic_temporal_benchmark"
  description: "Comparison of SNN and ANN on synthetic temporal data"
  output_dir: "./results/temporal_benchmark"

# Data settings
data:
  data_dir: "./data/synthetic"
  datasets:
    - "simple_synthetic_5class.npz"
    - "medium_synthetic_5class.npz"
    - "complex_synthetic_5class.npz"
  train_ratio: 0.8
  shuffle: true

# Models to compare
models:
  snn:
    config: "./configs/models/snn_synthetic.yaml"
  ann:
    config: "./configs/models/ann_synthetic.yaml"

# Hyperparameters
hyperparameters:
  batch_size: 32
  hidden_size: 128
  tau_m: 4.0
  tau_s: 1.0
  dropout_rate: 0.3
  epoch: 50  # Increased epochs to ensure consistent comparisons with spatial benchmark

# Optimizer settings
optimizer:
  optimizer_choice: "Adam" # Choose from Adam, AdamW, SGD
  Adam:
    lr: 0.001
  AdamW:
    lr: 0.001
  SGD:
    lr: 0.01

# Scheduler settings
scheduler:
  scheduler_choice: "none" # Choose from CosineAnnealingWarmRestarts, CyclicLR, MultiStepLR
  MultiStepLR:
    milestones: [30, 60, 90]
    gamma: 0.1
  CosineAnnealingWarmRestarts:
    T_0: 10
  CyclicLR:
    base_lr: 0.0001
    max_lr: 0.001
    step_size_up: 2000

# Evaluation metrics
metrics:
  - accuracy
  - loss
  - training_time
  - convergence_speed
  - inference_time

# Visualization settings
visualization:
  dpi: 150
  style: "seaborn-v0_8-paper"
  output_dir: "./results/temporal_benchmark/visualizations"
  formats:
    - "png"
    - "pdf"
  plots:
    - accuracy_curves
    - loss_curves
    - convergence_comparison
    - inference_time_comparison
